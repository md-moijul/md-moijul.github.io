## What to build

Refine the navigation and smooth scroll logic to ensure that cross-page anchor links and active section highlighting function correctly without hash-based routing.

## Acceptance criteria

- [x] `NavigationPanel.vue` correctly identifies when to perform a standard route change vs. a Lenis smooth scroll based on the current `route.path`.
- [x] Clicking a navigation link on the `/archive` page successfully redirects to `/` and then smooth-scrolls to the target section.
- [x] `useScrollSpy` continues to highlight the correct active section on the homepage without interference from the history mode change.

## Blocked by

- .scratch/v1/issues/11.2-transition-to-history-routing.md
