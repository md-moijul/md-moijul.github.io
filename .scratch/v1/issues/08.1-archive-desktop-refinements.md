Status: status/done

## Parent
.scratch/v1/issues/08-mobile-archive-layout.md

## What to build
Refine the ArchiveView on desktop to improve readability and navigation by making headers sticky within the card container and limiting the tech stack display.

1.  **Sticky Card Headers**: Update the card container in `ArchiveView.vue` to have a fixed height (e.g., `h-[calc(100vh-6rem)]`) and make both the main title section and the table column headers (`Year`, `Project`, etc.) sticky relative to the card's top.
2.  **Header Visuals**: Apply `bg-background/80` and `backdrop-blur-md` to sticky sections to ensure a clean visual overlap when project rows scroll underneath.
3.  **Tech Stack Flexing**: Update the tech stack badge container to use `flex-wrap` instead of horizontal locking. Implement a `max-height` (approx. `72px` for 3 rows) with `overflow-hidden`.
4.  **Expansion Logic**: Link the tech stack container's `max-height` to the existing `expandedProjects` state so that clicking a row expands both the description and the full stack.
5.  **Lenis & Mobile Safety**: Ensure `data-lenis-prevent` remains on the internal scrollable container. Verify that these changes only apply to the desktop view (`lg` breakpoint) to prevent regressions on mobile.

## Acceptance criteria
- [x] On desktop, the "All Projects" title remains visible at the top of the card while scrolling rows.
- [x] On desktop, the table column headers (`Year`, `Project`, etc.) stay pinned below the title section during scroll.
- [x] The tech stack column shows a maximum of 3 rows of badges by default on desktop.
- [x] Clicking a project row expands it to show the full tech stack and description.
- [x] Mobile card layout (Issue 08) remains fully functional and unaffected.
- [x] The global Lenis scroll on the HomeView is unaffected.

## Blocked by
.scratch/v1/issues/08-mobile-archive-layout.md

## Refinement (Planned)
Align `ArchiveView.vue` and its tests with `GEMINI.md` standards and clean up redundant logic.

### 1. Component Refactor
- **Data Decoupling**: Refactor to accept `projects` as a prop (standard mandate).
- **Scroll Optimization**: Consolidate nested overflow containers into a single functional layer.
- **Brittle Style Fix**: Replace magic numbers (e.g., `top-[104px]`) with CSS variables for robust stickiness.
- **CSS Cleanup**: Standardize scrollbar styles and remove redundant masks.

### 2. Test Consolidation
- Merge `ArchiveView.test.ts`, `ArchiveView.responsive.test.ts`, and `ArchiveView.refinement.test.ts` into a single, comprehensive suite organized by `describe` blocks.

### 3. Verification
- All existing functionality (sticky headers, blur, expansion, Lenis isolation) must remain identical.
- Unit tests must pass after consolidation.
