## Parent

16-interactive-stack-badges.md

## What to build

Restore text selection, link clicking, and badge interactivity within all components using the `Card` UI component. The noise overlay in `CardContent.vue` currently covers the entire card and blocks mouse events.

## Acceptance criteria

- [x] Add `pointer-events: none;` to the `:before` pseudo-element in `CardContent.vue`.
- [x] Add `z-index: 0;` to the `:before` pseudo-element to ensure it stays behind content.
- [x] Verify that text in Experience cards can be selected.
- [x] Verify that technology badges in Project cards are clickable and trigger stack filtering.

## Blocked by

None - can start immediately
