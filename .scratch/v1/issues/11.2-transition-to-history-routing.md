## What to build

Switch the application's routing mechanism from hash-based history (`#`) to standard browser history (History API).

## Acceptance criteria

- [x] `src/router/index.ts` uses `createWebHistory` instead of `createWebHashHistory`.
- [x] Navigation between the Home view and Archive view works without a `#` in the URL.
- [x] Direct navigation to `/archive` works (when combined with the 404 fallback from Infrastructure).

## Blocked by

- .scratch/v1/issues/11.1-routing-infrastructure.md
