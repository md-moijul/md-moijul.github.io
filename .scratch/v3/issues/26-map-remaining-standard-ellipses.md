## What to build

Using the proven mapping math from the primary blob, map the remaining standard SVG ellipses (the solid green one and the orange/white one) into CSS `.blob` elements. 

## Acceptance criteria

- [x] Map the solid green ellipse from the SVG to a new `.blob` element.
- [x] Map the orange/white ellipse from the SVG to a new `.blob` element, including its specific `linear-gradient`.
- [x] Calculate their exact `vw`/`vh` sizing and positions based on the 1440x900 viewBox.
- [x] Visually verify that these CSS blobs perfectly align with their SVG counterparts in the overlay.

## Blocked by

- .scratch/v3/issues/25-setup-overlay-and-map-primary-blob.md

## Status

Completed: Mapped the solid green and orange/white blobs mathematically.
